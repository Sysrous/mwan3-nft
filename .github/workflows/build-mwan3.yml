#
# GitHub Actions build script for mwan3-nft
# Targeted for OpenWrt 24.xx series, generating .ipk packages
#

name: Build mwan3-nft for OpenWrt 24.xx (ipk)

on:
  workflow_dispatch:
    inputs:
      openwrt_branch:
        description: 'OpenWrt Branch'
        required: true
        # 【关键修正】使用 openwrt-24.10 作为基础，确保生成 .ipk 包
        default: 'openwrt-24.10'

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Initialize build environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
        bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib \
        git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libglib2.0-dev libgmp3-dev libltdl-dev \
        libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libreadline-dev libssl-dev libtool lrzsz \
        mkisofs msmtp nano ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pip qemu-utils \
        rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev

    - name: Clone OpenWrt source code
      run: |
        git clone https://github.com/openwrt/openwrt.git -b ${{ github.event.inputs.openwrt_branch }} openwrt
        cd openwrt
        # 注入 mwan3-nft 的 feed
        echo "src-git mwan3nft https://github.com/dl12345/mwan3-nft.git" >> feeds.conf.default

    - name: Update and install feeds
      run: |
        cd openwrt
        rm -rf feeds/packages/net/mwan3
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: Load custom build configuration
      run: |
        if [ -f ../.config ]; then
          mv ../.config openwrt/.config
        else
          echo "ERROR: .config file not found. Please create it first."
          exit 1
        fi
        cd openwrt
        make defconfig

    - name: Download package dependencies
      run: |
        cd openwrt
        make package/mwan3/download V=s
        make package/luci-app-mwan3/download V=s

    - name: Compile mwan3 and its LuCI interface
      run: |
        cd openwrt
        echo "Compiling with $(nproc) threads..."
        make package/mwan3/compile V=s
        make package/luci-app-mwan3/compile V=s

    - name: Organize build artifacts
      id: organize
      run: |
        cd openwrt/bin/packages/
        echo "PACKAGES_PATH=$(pwd)" >> $GITHUB_ENV

    - name: Upload compiled packages
      uses: actions/upload-artifact@v4
      with:
        name: mwan3-nft-packages-ipk-24.10
        path: ${{ env.PACKAGES_PATH }}
